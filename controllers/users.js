const jwt = require('jsonwebtoken');

const User = require("../models/user");
const Session = require('../models/session');
const Comment = require('../models/comment');

module.exports = app => {
    // put our User routes here.
};
